<div class="section-{{ section.id }}">
<div class="page-width">
<div class="video-tabs-wrapper">
  <div class="video-full-tabs">
     <div class="video-text"> 
  <h2 class="video-title">Interior</h2> 
   {% if section.settings.mobile_video_title_image != blank %}
    <img src="{{ section.settings.mobile_video_title_image | image_url }}" alt="Videos" class="video-title-img-mobile">
  {% endif %}        
     </div>
  <div class="videos-tabs"> 
  <div class="video-tabs">
    <button class="tab-link2 active" data-tab="storage">Storage</button>
    <button class="tab-link2" data-tab="seatings">Seatings</button>
    <button class="tab-link2" data-tab="infotainment">Infotainment</button>
    <button class="tab-link2" data-tab="safety">Safety</button>
  </div>
     </div>     
  </div>
  <div class="tab-content2" id="storage">

    
{% if product.metafields.custom.interior_storage_image != blank %}
  <div class="images_all_out">
    {% assign images = product.metafields.custom.interior_storage_image.value | parse_json %}
      {% for image in images %}
      {% assign Counter = forloop.index %}  
      {% endfor %}
    {% for image in images %}
      {% assign Counter1 = forloop.index %}  
      {% if Counter1 <= 4 %}
      {% assign mod_check = forloop.index0 | modulo: 2 %}
      
      {% if mod_check == 0 %}
        <div class="parent-row">
      {% endif %}

      <div class="image-box-{{ forloop.index }} exterior-image">
        <img src="{{ image | image_url: width: 600 }}" alt="Exterior Image {{ forloop.index }}" />
      {% if Counter > 4 and Counter1 == 4 %}
      <div class="view-all-box" style="display: flex; justify-content: flex-end;">
        <a href="javascript:void(0);" onclick="openModal('{{ images | json | escape }}')" style="background: #c89c29; color: white; padding: 10px 20px; text-decoration: none; font-weight: bold;">
          VIEW ALL <br> {{ Counter }} PICTURES
        </a>
      </div>
    {% endif %}
      </div>

      {% assign mod_check_end = forloop.index0 | modulo: 2 %}
      {% if mod_check_end == 1 or forloop.last %}
        </div>
      {% endif %}
      {% endif %}
    {% endfor %}
  </div>
{% endif %}


  </div>
  <div class="tab-content2" id="seatings" style="display: none;">
    
   {% if product.metafields.custom.interior_seating_image != blank %}
  <div class="images_all_out">
    {% assign images = product.metafields.custom.interior_seating_image.value | parse_json %}
      {% for image in images %}
      {% assign Counter = forloop.index %}  
      {% endfor %}
    {% for image in images %}
      {% assign Counter1 = forloop.index %}  
      {% if Counter1 <= 4 %}
      {% assign mod_check = forloop.index0 | modulo: 2 %}
      
      {% if mod_check == 0 %}
        <div class="parent-row">
      {% endif %}

      <div class="image-box-{{ forloop.index }} exterior-image">
        <img src="{{ image | image_url: width: 600 }}" alt="Exterior Image {{ forloop.index }}" />
      {% if Counter > 4 and Counter1 == 4 %}
      <div class="view-all-box" style="display: flex; justify-content: flex-end;">
        <a href="javascript:void(0);" onclick="openModal('{{ images | json | escape }}')" style="background: #c89c29; color: white; padding: 10px 20px; text-decoration: none; font-weight: bold;">
          VIEW ALL <br> {{ Counter }} PICTURES
        </a>
      </div>
    {% endif %}
      </div>

      {% assign mod_check_end = forloop.index0 | modulo: 2 %}
      {% if mod_check_end == 1 or forloop.last %}
        </div>
      {% endif %}
      {% endif %}
    {% endfor %}
  </div>
{% endif %}

  </div>
  <div class="tab-content2" id="infotainment" style="display: none;">
  {% if product.metafields.custom.interior_info_image != blank %}
  <div class="images_all_out">
    {% assign images = product.metafields.custom.interior_info_image.value | parse_json %}
      {% for image in images %}
      {% assign Counter = forloop.index %}  
      {% endfor %}
    {% for image in images %}
      {% assign Counter1 = forloop.index %}  
      {% if Counter1 <= 4 %}
      {% assign mod_check = forloop.index0 | modulo: 2 %}
      
      {% if mod_check == 0 %}
        <div class="parent-row">
      {% endif %}

      <div class="image-box-{{ forloop.index }} exterior-image">
        <img src="{{ image | image_url: width: 600 }}" alt="Exterior Image {{ forloop.index }}" />
      {% if Counter > 4 and Counter1 == 4 %}
      <div class="view-all-box" style="display: flex; justify-content: flex-end;">
        <a href="javascript:void(0);" onclick="openModal('{{ images | json | escape }}')" style="background: #c89c29; color: white; padding: 10px 20px; text-decoration: none; font-weight: bold;">
          VIEW ALL <br> {{ Counter }} PICTURES
        </a>
      </div>
    {% endif %}
      </div>

      {% assign mod_check_end = forloop.index0 | modulo: 2 %}
      {% if mod_check_end == 1 or forloop.last %}
        </div>
      {% endif %}
      {% endif %}
    {% endfor %}
  </div>
{% endif %}
  </div>
  <div class="tab-content2" id="safety" style="display: none;">
    {% if product.metafields.custom.interior_safety_image != blank %}
  <div class="images_all_out">
    {% assign images = product.metafields.custom.interior_safety_image.value | parse_json %}
      {% for image in images %}
      {% assign Counter = forloop.index %}  
      {% endfor %}
    {% for image in images %}
      {% assign Counter1 = forloop.index %}  
      {% if Counter1 <= 4 %}
      {% assign mod_check = forloop.index0 | modulo: 2 %}
      
      {% if mod_check == 0 %}
        <div class="parent-row">
      {% endif %}

      <div class="image-box-{{ forloop.index }} exterior-image">
        <img src="{{ image | image_url: width: 600 }}" alt="Exterior Image {{ forloop.index }}" />
      {% if Counter > 4 and Counter1 == 4 %}
      <div class="view-all-box" style="display: flex; justify-content: flex-end;">
        <a href="javascript:void(0);" onclick="openModal('{{ images | json | escape }}')" style="background: #c89c29; color: white; padding: 10px 20px; text-decoration: none; font-weight: bold;">
          VIEW ALL <br> {{ Counter }} PICTURES
        </a>
      </div>
    {% endif %}
      </div>

      {% assign mod_check_end = forloop.index0 | modulo: 2 %}
      {% if mod_check_end == 1 or forloop.last %}
        </div>
      {% endif %}
      {% endif %}
    {% endfor %}
  </div>
{% endif %}
    
  </div>
</div>
</div>
</div>  


<div id="viewAllModal1" class="view-all-modal" style="display: none;">
  <div class="modal-content">
    <span class="close-modal" onclick="closeModal()">&times;</span>
    <div class="all-images-grid" id="allImagesContainer1">
      <!-- JS will insert tab-specific images here -->
    </div>
  </div>
</div>

<!--<div id="viewAllModal1" class="view-all-modal" style="display: none;">
  <div class="modal-content">
    <span class="close-modal" onclick="closeModal()">&times;</span>
    <div class="all-images-grid">
      {% for image in images %}
        <img src="{{ image | image_url: width: 800 }}" alt="All Exterior Image {{ forloop.index }}" />
      {% endfor %}
    </div>
  </div>
</div>-->

<script>
function openModal(imagesJson) {
    const container = document.getElementById('allImagesContainer1');
    const images = JSON.parse(imagesJson);
    container.innerHTML = ''; // Clear previous images

    images.forEach((src, index) => {
      const img = document.createElement('img');
      img.src = `${src}?width=800`;
      img.alt = `Exterior Image ${index + 1}`;
      container.appendChild(img);
    });

    document.getElementById('viewAllModal1').style.display = 'block';
  }


  function closeModal() {
    document.getElementById("viewAllModal1").style.display = "none";
  }

  // Optional: Close when clicking outside modal content
  window.onclick = function(event) {
    var modal = document.getElementById("viewAllModal1");
    if (event.target == modal) {
      closeModal();
    }
  }
</script>

<style>
.view-all-modal {
  position: fixed;
  z-index: 1000;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  overflow: auto;
  background-color: rgba(0,0,0,0.8);
}

.video-title-img-mobile {
  display: none;
  width: 100%;
  height: auto;
}
.modal-content {
  background-color: #fefefe;
  margin: 5% auto;
  padding: 20px;
  max-width: 90%;
  border-radius: 8px;
}

.close-modal {
  color: #aaa;
  float: right;
  font-size: 28px;
  cursor: pointer;
}

.close-modal:hover {
  color: black;
}

.all-images-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  justify-content: center;
}

.all-images-grid img {
  max-width: 50%;
  height: auto;
  border-radius: 5px;
}

  
.video-tabs-wrapper {
/*  background-color: #e6e4ce; */
  padding: 20px 0px;
  font-family: Arial, sans-serif;
  border-top: 5px solid #6B8D50;
}

.video-title {
  color: #567336;
  font-weight: bold;
  font-size: 50px;
  margin-bottom: 20px;
  font-family:'VVDS Fifties';
  margin-top:0px;
}
.video-text {
    width: 15%;
}
.video-tabs {
  display: flex;
  gap: 15px;
  margin-bottom: 15px;
}
  .videos-tabs {
    width: 85%;
}

.tab-link2 {
  background-color: #d6d7bc;
  border: none;
  padding: 10px 30px;
  cursor: pointer;
  font-size: 14px;
  color: #567336;
  transition: background-color 0.3s;
  font-family: Eczar;
    font-size: 20px;
  width:25%;
}

.tab-link2.active {
  background-color: #688c45;
  color: white;
  font-family: Eczar;
  font-size: 20px;
}

.tab-content2 {
/*  background: white;
  padding: 20px;
  border: 1px solid #d6d7bc; */
}

@media (max-width: 768px) {
  .video-title {
    display: none;
  }

  .video-title-img-mobile {
    display: inline-block;
    width: 100%;
    padding: 20px 0px;
  }
}  
</style>

<script>
document.querySelectorAll('.tab-link2').forEach(function(button) {
  button.addEventListener('click', function() {
    // Remove active class from all tabs
    document.querySelectorAll('.tab-link2').forEach(function(btn) {
      btn.classList.remove('active');
    });
    // Add active class to the clicked tab
    this.classList.add('active');

    // Hide all contents
    document.querySelectorAll('.tab-content2').forEach(function(content) {
      content.style.display = 'none';
    });

    // Show the clicked content
    var tabId = this.getAttribute('data-tab');
    document.getElementById(tabId).style.display = 'block';
  });
});
</script>


{% schema %}
{
  "name": "Interior Image Section",
    "settings": [
    {
      "type": "image_picker",
      "id": "mobile_video_title_image",
      "label": "Mobile Video Title Image"
    }
  ],
  "blocks": [
    {
      "type": "tab",
      "name": "Tab",
      "settings": [
        {
          "type": "text",
          "id": "tab_title",
          "label": "Tab Title"
        },
        {
          "type": "text",
          "id": "item_1",
          "label": "Grid Item 1"
        }
      ]
    }
  ],
  "max_blocks": 6,
  "presets": [
    {
      "name": "Interior Images",
      "category": "Custom"
    }
  ]
}
{% endschema %}
