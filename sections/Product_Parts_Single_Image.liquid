<div class="section-{{ section.id }}">
<div class="page-width">
<div class="product-parts-single-image">
  {% if product.metafields.custom.parts_single_image %}
    <img 
      src="{{ product.metafields.custom.parts_single_image | image_url }}" 
      alt="{{ product.metafields.custom.parts_single_image.alt | escape }}" 
      loading="lazy"
    >
  {% else %}
    <p>No image available.</p>
  {% endif %}
</div>
</div>
</div>  

{% schema %}
{
  "name": "Product Parts Single", 
  "settings": [],
  "presets": [
    {
      "name": "Product Parts Single",
      "category": "Custom"
    }
  ]
}
{% endschema %}
