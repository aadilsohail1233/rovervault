{% schema %}
{
  "name": "Home Product Arts",
  "settings": [],
  "blocks": [
    {
      "type": "product_card",
      "name": "Product",
      "settings": [
        {
          "type": "product",
          "id": "product",
          "label": "Select product"
        }
      ]
    }
  ],
  "max_blocks": 10,
  "presets": [{
    "name": "Home Product Arts"
  }]
}
{% endschema %}
    <div class="page-width">
    <div class="collection-slider-section padding-bottom-zero">
      <div class="swiper mySwiper3">
        <div class="swiper-wrapper">
          {% for block in section.blocks %}
            {% assign product = block.settings.product %}
            {% if product %}
              <div class="swiper-slide">
                 <div class="content">
                <div class="cards">
                     <div class="card"> 

                      <div class="card__side card__side--front card__side--front-1"> 
                      <div class="product-col-1">
                        <div class="product-avaliable">Avaliable</div>
                         <div class="relative_img">
                         <img alt="{{ product.title }}" class="prd-img-others" src="{{ product.featured_image | img_url: 'large' }}"/>
                         <div class="badge left">
                           Art
                            {% comment %}
                          {{ product.collections.first.title | default: "Vehicle" }}
                          {% endcomment %}
                         </div>
                         <div class="badge rights">
                              <a href="#"> 
                          <img src="https://cdn.shopify.com/s/files/1/0694/0314/8465/files/Group_620.png?v=1745422926" />
                              </a>
                              </div>   
                         </div>
                          <div class="product-col-title">{{ product.title }}</div>
                        <div class="button-product margin-button-bottom"><a href="{{ product.url }}" class="button--main">View full listing</a></div>
                      </div>
                      </div>  
                    <div class="card__side card__side--back card__side--back-1">
                     <div class="product-col-2">
                        <div class="product-avaliable">Avaliable</div>
                        <div class="relative_img">
                          {% assign second_image = product.images[1] %}
                         <img alt="{{ product.title }}" class="prd-img" src="{% if second_image %}{{ second_image | img_url: 'large' }}{% else %}{{ product.featured_image | img_url: 'large' }}{% endif %}" />
                         <div class="badge right">
                          {{ product.collections.first.title | default: "Vehicle" }}
                         </div>
                          <div class="badge lefts">
                          <a href="#">  
                          <img src="https://cdn.shopify.com/s/files/1/0694/0314/8465/files/Group_620.png?v=1745422926" />
                          </a>  
                         </div>  
                        </div>
                        <div class="product-col-title white-color">{{ product.title }}</div>
                       <div class="button-product margin-button-bottom"><a href="{{ product.url }}" class="button--main">View full listing</a></div>
                     </div>   
                    </div> 
                   
                 </div>
                </div>
                </div>
              </div>
            {% endif %}
          {% endfor %}
        </div>
    

      </div>
                    <!-- Slider navigation -->
  <div class="swiper-controls-wrap">
    <div class="slider-controls">
  <div class="swiper-button-prev3">
<svg width="67" height="46" viewBox="0 0 67 46" fill="none" xmlns="http://www.w3.org/2000/svg">
<path d="M24 3L4 23M4 23L24 43M4 23H64" stroke="#6B8D50" stroke-width="5" stroke-linecap="round"/>
</svg>

</div>
  <div class="swiper-pagination3"></div>
  <div class="swiper-button-next3">
<svg width="67" height="46" viewBox="0 0 67 46" fill="none" xmlns="http://www.w3.org/2000/svg">
<path d="M43 3L63 23M63 23L43 43M63 23H3" stroke="#6B8D50" stroke-width="5" stroke-linecap="round"/>
</svg>

</div>
</div>
  </div>
    </div>

    </div>
    
    <!-- Swiper JS (Add this only once per theme if not already included) -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper/swiper-bundle.min.css" />
    <script src="https://cdn.jsdelivr.net/npm/swiper/swiper-bundle.min.js"></script>
    
    <script>
document.addEventListener('DOMContentLoaded', function () {
  new Swiper('.mySwiper3', {
    slidesPerView: 3,
    spaceBetween: 30,
    loop: true,
    pagination: {
      el: ".swiper-pagination3",
      clickable: true,
    },
    navigation: {
      nextEl: ".swiper-button-next3",
      prevEl: ".swiper-button-prev3",
    },
    breakpoints: {
      // Screens up to 767px will use 1 slide per view
      0: {
        slidesPerView: 1
      },
      // From 768px and above
      768: {
        slidesPerView: 3
      }
    }
  });
});
    </script>

    <script>
document.addEventListener('DOMContentLoaded', function () {
  const flipToBackButtons = document.querySelectorAll('.badge.lefts a');
  const flipToFrontButtons = document.querySelectorAll('.badge.rights a');

  // Flip to back
  flipToBackButtons.forEach(button => {
    button.addEventListener('click', function (e) {
      e.preventDefault();
      if (window.innerWidth <= 767) {
        const card = this.closest('.card');
        card.classList.remove('frontflipped');
        card.classList.add('flipped');
      }
    });
  });

  // Flip to front
  flipToFrontButtons.forEach(button => {
    button.addEventListener('click', function (e) {
      e.preventDefault();
      if (window.innerWidth <= 767) {
        const card = this.closest('.card');
        card.classList.remove('flipped');
        card.classList.add('frontflipped');
      }
    });
  });
});
</script>
    
    <style>
.main-product-section {
    display: flex;
   gap:4rem; 
}
      .desktop-section-hide {
    display: none !important;
}
.white-color {
    color: white !important;
        border-color:white !important
}
.button-product .button--main {
    font-family: 'Eczar';
    font-size: 20px;
}
  .button-product {
    display: flex;
    position: relative;
    margin: 0 auto;
    justify-content: center;
    margin-top: 4rem;
}
.mar-bottom {
    margin-bottom: 4rem !important;
}
.relative_img {
    position: relative;
}
      .product-b-top {
    border-top: 5px solid #6B8D50;
}
      .product-b-bottom {
    border-bottom: 5px solid #6B8D50;
}
      .badge.left {
    position: absolute;
    left: 15px;
    top: 20px;
    background-color: #C18A14;
    color: white;
    border-radius: 0px;
    padding: 10px 15px;
    font-family: 'Eczar';
    font-size: 16px;
}
      .cl-description {
    font-family: 'Eczar';
    font-size: 20px;
      color:#6B8D50;     
}
      .product-details-col {
    display: flex;
    justify-content: space-between;
    width: 85%;
    margin: 0 auto;
    align-items:center;    
}
            .badge.right {
    position: absolute;
    left: 15px;
    top: 20px;
    background-color: #C18A14;
    color: white;
    border-radius: 0px;
    padding: 10px 15px;
    font-family: 'Eczar';
    font-size: 16px;
}

      .product-avaliable {
    background-color: #6B8D50;
    text-align: center;
    color: white;
    font-family: VVDS Fifties;
    font-size: 24px;
}
  .main-product-section .prd-img {
    width: 100%;
    height: 300px;
}
 .product-col-1{
   width:100%;
 border: 5px solid rgba(var(--color-secondary-button-text));
 }     
.product-col-2{
  width:100%;
   border: 5px solid rgba(var(--color-secondary-button-text));
  background-color:#6B8D50;
}


  .swiper-controls-wrap .swiper-button-prev3{
    display: block  !important;
}

        .swiper-controls-wrap .swiper-button-prev3 svg{
    width: 5rem;
}
  .swiper-controls-wrap .swiper-button-prev3:after{
    display: none  !important;
}


.swiper-controls-wrap .swiper-button-next3{
    display: block  !important;
}

      .swiper-controls-wrap .swiper-button-next3 svg{
    width: 5rem;
}


.swiper-controls-wrap .swiper-button-next3:after{
    display: none  !important;
}

.swiper-pagination-bullet{
  width: 2.25rem;
  height: 2.25rem;
  border: 5px solid #6B8D50;
  background: transparent !important;
}

.swiper-pagination-bullet-active{
 
  position: relative;
}

.swiper-pagination-bullet-active:after{
  content: '';
    width: .75rem;
    height: 0.75rem;
    border-radius: 50%;
    background: #6B8D50 !important;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    position: absolute;
}
.swiper-button-next3.swiper-pagination-bullets.swiper-pagination-horizontal{
  bottom: auto;
  left: 48%;
  transform: translate(-45%, 0);
  max-width: 50%;
  z-index: 1;
}
@media only screen and (max-width: 767px) {

.main-product-section {
    display: block;
    width: 96%;
    margin: 0 auto;
}
  .mobile-section-hide {
    display: none;
}
  .desktop-section-hide {
        display: flex !important;
        width: 100%;
        text-align: center;
        margin-bottom: 3rem;
        margin-top: 0px !important;
    }
 .cl-title{
   font-size: 30px;
 } 
 .cl-description{
       font-size: 15px;
 } 
  .swiper-controls-wrap .swiper-button-prev3 {
    display: none !important;
}
  .swiper-controls-wrap .swiper-button-next3 {
    display: none !important;
}
}
    </style>

